{
  "plugin_id": "mqtt",
  "version": "0.1.0",
  "permissions": [
    "network",
    "secrets"
  ],
  "kernel_compatibility_range": {
    "min": "0.1.0",
    "max": "*"
  },
  "binary": "mqtt",
  "events": [],
  "files": [
    "mqtt",
    "plugin.json"
  ],
  "actions": [
    {
      "name": "mqtt_connect",
      "description": "Open the MQTT broker connection used by every other mqtt action: broker_host (+ optional port, client_id, username/password resolved vault-first via secrets, keepalive, CA/client certs for TLS or mTLS). One connection per plugin instance; connecting again replaces it.",
      "risk": "medium",
      "permission": "network",
      "input": {
        "type": "object",
        "properties": {
          "broker_host": {
            "type": "string",
            "description": "MQTT broker hostname or IP"
          },
          "broker_port": {
            "type": "integer",
            "default": 1883,
            "description": "MQTT broker port"
          },
          "client_id": {
            "type": "string",
            "description": "MQTT client ID (default: auto-generated)"
          },
          "username": {
            "type": "string",
            "description": "MQTT username"
          },
          "password": {
            "type": "string",
            "description": "MQTT password (vault-first via secrets plugin)"
          },
          "keepalive_secs": {
            "type": "integer",
            "default": 60,
            "description": "MQTT keepalive in seconds"
          },
          "ca_cert_path": {
            "type": "string",
            "description": "Path to CA certificate for TLS"
          },
          "client_cert_path": {
            "type": "string",
            "description": "Path to client certificate for mTLS"
          },
          "client_key_path": {
            "type": "string",
            "description": "Path to client key for mTLS"
          }
        },
        "required": ["broker_host"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "connected": {
            "type": "boolean"
          },
          "broker": {
            "type": "string"
          }
        },
        "required": ["connected"]
      }
    },
    {
      "name": "mqtt_disconnect",
      "description": "Close the current broker connection and drop all active subscriptions. Device state already published to the broker is unaffected.",
      "risk": "medium",
      "input": {
        "type": "object",
        "properties": {}
      },
      "output": {
        "type": "object",
        "properties": {
          "disconnected": {
            "type": "boolean"
          }
        },
        "required": ["disconnected"]
      }
    },
    {
      "name": "mqtt_publish",
      "description": "Publish a payload to an MQTT topic (qos, retain, encoding for base64 protobuf). Reaches whatever hardware listens on that topic, so a wrong topic actuates a real device; retain=true leaves the message on the broker for future subscribers.",
      "risk": "high",
      "requires_confirmation": true,
      "permission": "network",
      "input": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string",
            "description": "MQTT topic to publish to"
          },
          "payload": {
            "type": "string",
            "description": "Message payload (text or base64-encoded protobuf)"
          },
          "qos": {
            "type": "integer",
            "enum": [0, 1, 2],
            "default": 0,
            "description": "MQTT QoS level"
          },
          "retain": {
            "type": "boolean",
            "default": false,
            "description": "Retain flag"
          },
          "encoding": {
            "type": "string",
            "enum": ["utf8", "base64", "protobuf"],
            "default": "utf8",
            "description": "Payload encoding"
          }
        },
        "required": ["topic", "payload"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "published": {
            "type": "boolean"
          },
          "message_id": {
            "type": "integer"
          }
        },
        "required": ["published"]
      }
    },
    {
      "name": "mqtt_subscribe",
      "description": "Subscribe to an MQTT topic filter (wildcards allowed, e.g. vynkor/+/telemetry); stream=true delivers messages over R6 streaming instead of buffering. Adds broker-side subscription state.",
      "risk": "medium",
      "permission": "network",
      "input": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string",
            "description": "MQTT topic to subscribe to"
          },
          "qos": {
            "type": "integer",
            "enum": [0, 1, 2],
            "default": 0,
            "description": "MQTT QoS level"
          },
          "stream": {
            "type": "boolean",
            "default": true,
            "description": "Stream messages via R6 streaming"
          }
        },
        "required": ["topic"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "subscribed": {
            "type": "boolean"
          },
          "subscription_id": {
            "type": "string"
          }
        },
        "required": ["subscribed"]
      }
    },
    {
      "name": "mqtt_unsubscribe",
      "description": "Stop receiving messages for a topic filter previously passed to mqtt_subscribe.",
      "risk": "medium",
      "input": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string",
            "description": "MQTT topic to unsubscribe from"
          }
        },
        "required": ["topic"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "unsubscribed": {
            "type": "boolean"
          }
        },
        "required": ["unsubscribed"]
      }
    },
    {
      "name": "mqtt_status",
      "description": "Report broker connection state: connected or not, broker address, client id and active subscription count. Read-only.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {}
      },
      "output": {
        "type": "object",
        "properties": {
          "connected": {
            "type": "boolean"
          },
          "broker": {
            "type": "string"
          },
          "uptime_ms": {
            "type": "integer"
          },
          "subscriptions": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "messages_sent": {
            "type": "integer"
          },
          "messages_received": {
            "type": "integer"
          }
        },
        "required": ["connected"]
      }
    },
    {
      "name": "mqtt_device_list",
      "description": "List IoT devices discovered over the Vynkor Device Protocol (WiFi/ESP, Zigbee2MQTT, Thread border router) with their ids and last-seen state. Read-only.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {}
      },
      "output": {
        "type": "object",
        "properties": {
          "devices": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "device_id": {
                  "type": "string"
                },
                "device_type": {
                  "type": "string"
                },
                "online": {
                  "type": "boolean"
                },
                "last_seen": {
                  "type": "integer"
                },
                "firmware": {
                  "type": "string"
                }
              },
              "required": ["device_id"]
            }
          }
        },
        "required": ["devices"]
      }
    },
    {
      "name": "mqtt_device_info",
      "description": "Get one discovered device's details by device_id: type, firmware version, capabilities and last-seen timestamp. Read-only.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          }
        },
        "required": ["device_id"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          },
          "device_type": {
            "type": "string"
          },
          "online": {
            "type": "boolean"
          },
          "firmware": {
            "type": "string"
          },
          "ip": {
            "type": "string"
          },
          "wifi_rssi": {
            "type": "integer"
          },
          "uptime_s": {
            "type": "integer"
          },
          "supported_commands": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "pins": {
            "type": "array",
            "items": {
              "type": "object"
            }
          }
        },
        "required": ["device_id"]
      }
    },
    {
      "name": "mqtt_device_command",
      "description": "Send a VDP command to a device by device_id (command name plus a params object, e.g. set_relay {pin, state}). This actuates physical hardware — a relay, lock, light or motor moves in the real world.",
      "risk": "high",
      "requires_confirmation": true,
      "permission": "network",
      "input": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          },
          "command": {
            "type": "string"
          },
          "params": {
            "type": "object"
          },
          "timeout_ms": {
            "type": "integer",
            "default": 5000
          }
        },
        "required": ["device_id", "command"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean"
          },
          "error_code": {
            "type": "integer"
          },
          "error_message": {
            "type": "string"
          },
          "data": {
            "type": "object"
          },
          "execution_ms": {
            "type": "integer"
          }
        },
        "required": ["success"]
      }
    },
    {
      "name": "mqtt_device_telemetry",
      "description": "Read buffered telemetry for a device: optional stream name, since (unix seconds) and limit. Read-only.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          },
          "stream": {
            "type": "string"
          },
          "since": {
            "type": "integer",
            "description": "Unix timestamp (return data since this time)"
          },
          "limit": {
            "type": "integer",
            "default": 100
          }
        },
        "required": ["device_id"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          },
          "telemetry": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "timestamp": {
                  "type": "integer"
                },
                "stream": {
                  "type": "string"
                },
                "data": {
                  "type": "object"
                }
              }
            }
          }
        },
        "required": ["device_id"]
      }
    },
    {
      "name": "mqtt_ota_update",
      "description": "Flash new firmware onto a device by pointing it at firmware_url. Irreversible from this side and unrecoverable if the image is wrong for the target — a failed OTA can leave the hardware unbootable and unreachable over the network.",
      "risk": "critical",
      "requires_confirmation": true,
      "permission": "network",
      "input": {
        "type": "object",
        "properties": {
          "device_id": {
            "type": "string"
          },
          "firmware_url": {
            "type": "string",
            "description": "URL to firmware binary"
          },
          "firmware_version": {
            "type": "string"
          }
        },
        "required": ["device_id", "firmware_url"],
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "properties": {
          "started": {
            "type": "boolean"
          },
          "ota_id": {
            "type": "string"
          }
        },
        "required": ["started"]
      }
    }
  ],
  "config_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "MQTT_PLUGIN_DEFAULT_BROKER": {
        "type": "string",
        "description": "Default MQTT broker URL (mqtt://host:port)"
      },
      "MQTT_PLUGIN_DEFAULT_USERNAME": {
        "type": "string",
        "description": "Default MQTT username"
      },
      "MQTT_PLUGIN_DEFAULT_PASSWORD": {
        "type": "string",
        "description": "Default MQTT password (vault-first via secrets)"
      },
      "MQTT_PLUGIN_CA_CERT_PATH": {
        "type": "string",
        "description": "Path to CA certificate for TLS"
      },
      "MQTT_PLUGIN_CLIENT_CERT_PATH": {
        "type": "string",
        "description": "Path to client certificate for mTLS"
      },
      "MQTT_PLUGIN_CLIENT_KEY_PATH": {
        "type": "string",
        "description": "Path to client key for mTLS"
      },
      "MQTT_PLUGIN_STREAM_IDLE_TIMEOUT_MS": {
        "type": "integer",
        "default": 300000,
        "description": "Stream idle timeout in milliseconds (default: 5 min)"
      },
      "MQTT_PLUGIN_TOPIC_PREFIX": {
        "type": "string",
        "default": "vynkor",
        "description": "MQTT topic prefix (default: vynkor)"
      }
    },
    "additionalProperties": true
  }
}
