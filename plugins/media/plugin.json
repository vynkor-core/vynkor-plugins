{
  "plugin_id": "media",
  "version": "0.1.0",
  "permissions": [],
  "kernel_compatibility_range": {
    "min": "0.1.0",
    "max": "*"
  },
  "binary": "media",
  "events": [],
  "files": [
    "media",
    "plugin.json"
  ],
  "actions": [
    {
      "name": "media_play",
      "description": "Start or force-resume playback on an MPRIS player (Spotify, mpv, VLC, browser tab, ...); if `uri` is given, opens/loads that media first via OpenUri, then plays. Unlike media_play_pause, this always ends in the playing state rather than toggling.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name (org.mpris.MediaPlayer2.<name>); omit for the default player."
          },
          "uri": {
            "type": "string",
            "description": "Optional media URI to open before playing."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_pause",
      "description": "Pause playback on an MPRIS player without changing track or position; always ends paused (use media_play to resume, media_play_pause to toggle).",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_play_pause",
      "description": "Toggle an MPRIS player between playing and paused (whichever it currently isn't) and report the resulting `playing` state; use media_play or media_pause instead when you need a specific state rather than a toggle.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          },
          "playing": {
            "type": "boolean",
            "description": "Playback state after the toggle."
          }
        },
        "required": [
          "ok",
          "playing"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_next",
      "description": "Skip to the next track/item on an MPRIS player's current playlist.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_prev",
      "description": "Skip to the previous track/item on an MPRIS player's current playlist.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_stop",
      "description": "Stop playback on an MPRIS player entirely; unlike media_pause, some players reset or drop the playback position on stop rather than keeping it for resume.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_seek",
      "description": "Jump to an absolute position (`position_ms`, measured from the start of the current track) on an MPRIS player; for a relative jump forward/back from wherever playback is now, use media_seek_relative instead.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "position_ms": {
            "type": "integer",
            "minimum": 0,
            "description": "Absolute seek target in milliseconds from the start of the track."
          },
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "required": [
          "position_ms"
        ],
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "position_ms": {
            "type": "integer",
            "description": "Resulting position in milliseconds."
          }
        },
        "required": [
          "position_ms"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_seek_relative",
      "description": "Jump forward or backward by a signed `offset_ms` from the current playback position on an MPRIS player (negative rewinds; result clamps at 0); for a jump to a specific absolute timestamp, use media_seek instead.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "offset_ms": {
            "type": "integer",
            "description": "Signed offset in milliseconds from the current position (negative seeks backward; result clamps at 0)."
          },
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "required": [
          "offset_ms"
        ],
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "position_ms": {
            "type": "integer",
            "description": "Requested target position in milliseconds after applying the offset."
          }
        },
        "required": [
          "position_ms"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_volume",
      "description": "Set an MPRIS player's volume via `level`, accepted as either a 0.0-1.0 fraction or a 0-100 integer percentage — note the ambiguity: JSON `1` (integer) means 1%, while `1.0` (float) means 100%; send a float for a fraction and an integer for a percentage.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "level": {
            "description": "Target volume: a 0.0..1.0 fraction or a 0..100 integer percentage.",
            "type": [
              "number",
              "integer"
            ],
            "minimum": 0,
            "maximum": 100
          },
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "required": [
          "level"
        ],
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "volume": {
            "type": "number",
            "description": "Resulting volume as a 0.0..1.0 fraction."
          }
        },
        "required": [
          "volume"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_status",
      "description": "Get full playback state for an MPRIS player in one call: status (Playing/Paused/Stopped), track metadata (title/artist/album/...), volume, position_ms, playback rate, shuffle, and loop_status — use this to check what's playing before deciding to play/pause/skip.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name queried."
          },
          "status": {
            "type": "string",
            "enum": [
              "Playing",
              "Paused",
              "Stopped"
            ]
          },
          "metadata": {
            "type": "object",
            "description": "Normalized MPRIS metadata (title, artists, album, length_micros, track_id, art_url).",
            "additionalProperties": true
          },
          "volume": {
            "type": "number",
            "description": "Volume as a 0.0..1.0 fraction."
          },
          "position_ms": {
            "type": "integer",
            "description": "Current playback position in milliseconds."
          },
          "rate": {
            "type": "number",
            "description": "Playback rate (1.0 normal, 0.0 paused)."
          },
          "shuffle": {
            "type": "boolean",
            "description": "Shuffle enabled."
          },
          "loop_status": {
            "type": "string",
            "enum": [
              "None",
              "Track",
              "Playlist"
            ],
            "description": "Loop mode."
          }
        },
        "required": [
          "player",
          "status",
          "metadata",
          "volume",
          "position_ms",
          "rate",
          "shuffle",
          "loop_status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_list_players",
      "description": "List MPRIS player names currently on the session D-Bus (org.mpris.MediaPlayer2.<name>) — use to discover the `player` value for other media_* actions when more than one app is playing audio.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {}
      },
      "output": {
        "type": "object",
        "properties": {
          "players": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "MPRIS player names (org.mpris.MediaPlayer2.<name>) available on the session bus."
          }
        },
        "required": [
          "players"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_shuffle",
      "description": "Turn shuffle mode on or off (`enabled`, alias `shuffle`) for an MPRIS player.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable shuffle (true) or disable (false). Alias `shuffle` also accepted."
          },
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "required": [
          "enabled"
        ],
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "shuffle": {
            "type": "boolean"
          }
        },
        "required": [
          "shuffle"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_loop",
      "description": "Set repeat mode on an MPRIS player: none (stop at end), track (repeat the current track), or playlist (repeat the whole queue).",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "none",
              "track",
              "playlist",
              "None",
              "Track",
              "Playlist"
            ],
            "description": "Loop mode: none / track (single) / playlist (all)."
          },
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for the default player."
          }
        },
        "required": [
          "mode"
        ],
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "loop_status": {
            "type": "string",
            "enum": [
              "None",
              "Track",
              "Playlist"
            ]
          }
        },
        "required": [
          "loop_status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_raise",
      "description": "Bring an MPRIS player's window to the foreground/focus via its Raise method; only changes window focus, not playback.",
      "risk": "low",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "media_quit",
      "description": "Quit the MPRIS player application entirely (its Quit method) — unlike media_stop, which only halts playback, this closes the whole app and can lose any unsaved queue/session state in it.",
      "risk": "medium",
      "input": {
        "type": "object",
        "properties": {
          "player": {
            "type": "string",
            "description": "MPRIS player name; omit for default player."
          }
        },
        "additionalProperties": true
      },
      "output": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          }
        },
        "required": [
          "ok"
        ],
        "additionalProperties": false
      }
    }
  ],
  "config_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "MEDIA_PLUGIN_PLAYERS": {
        "type": "string",
        "description": "Allowlist mode (default allow-all): comma-separated MPRIS player names (org.mpris.MediaPlayer2.<name>) this plugin may control."
      },
      "MEDIA_PLUGIN_DEFAULT_PLAYER": {
        "type": "string",
        "description": "MPRIS player name used when a request omits `player`. Unset = first available player."
      }
    },
    "additionalProperties": true
  }
}
