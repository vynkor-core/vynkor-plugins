# capture plugin — no required config; every setting below has a working default.

# Where screenshots/recordings are written. Default: ~/.local/share/vyn/capture/
# CAPTURE_PLUGIN_DIR: /home/you/.local/share/vyn/capture
